defmodule CALCULATOR do

  def addition(num_one,num_two) do
    num_one+num_two
  end

  def subtaction(num_one,num_two) do
    num_one-num_two
  end

  def multiplcation(num_one,num_two) do
    num_one*num_two
  end

  def division(num_one,num_two) do
    num_one/num_two
  end

  def remender(num_one,num_two) do
    rem(num_one,num_two)
  end

end
